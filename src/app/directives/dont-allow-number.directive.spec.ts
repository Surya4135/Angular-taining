import { DontAllowNumberDirective } from './dont-allow-number.directive';

describe('DontAllowNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new DontAllowNumberDirective();
    expect(directive).toBeTruthy();
  });
});
