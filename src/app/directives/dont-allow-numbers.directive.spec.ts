import { DontAllowNumbersDirective } from './dont-allow-numbers.directive';

describe('DontAllowNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new DontAllowNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
