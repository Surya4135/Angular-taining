import { HoverDescriptionDirective } from './hover-description.directive';

describe('HoverDescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverDescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
