import { DateModifierPipe } from './date-modifier.pipe';

describe('DateModifierPipe', () => {
  it('create an instance', () => {
    const pipe = new DateModifierPipe();
    expect(pipe).toBeTruthy();
  });
});
